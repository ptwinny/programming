// objects
Book(title, author, yearPublished, genre) {
  this.title = title;
  this.author = author;
  this.yearPublished = yearPublished;
  this.genre = genre;
  this.review = review;
};


const userGoal = prompt("What is your ultimate goal?");

const goal = userGoal;

console.log("Your goal: " + goal);

